import { describe, expect, it } from "vitest";
import { challenge22 } from "./challenge22";

describe("challenge 22 placeholder", () => {
  it("should return input by default", () => {
    expect(challenge22(1)).toBe(1);
  });
});