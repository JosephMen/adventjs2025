import { describe, expect, it } from "vitest";
import { challenge21 } from "./challenge21";

describe("challenge 21 placeholder", () => {
  it("should return input by default", () => {
    expect(challenge21(1)).toBe(1);
  });
});