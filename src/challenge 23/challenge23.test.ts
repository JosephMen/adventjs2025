import { describe, expect, it } from "vitest";
import { challenge23 } from "./challenge23";

describe("challenge 23 placeholder", () => {
  it("should return input by default", () => {
    expect(challenge23(1)).toBe(1);
  });
});