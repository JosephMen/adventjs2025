import { describe, expect, it } from "vitest";
import { challenge25 } from "./challenge25";

describe("challenge 25 placeholder", () => {
  it("should return input by default", () => {
    expect(challenge25(1)).toBe(1);
  });
});